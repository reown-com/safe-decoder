# OpenAI API Key - Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here 
# Google Sheets Service Account Credentials (JSON content)
# Create a service account, generate a JSON key, and share the sheet with the service account email.
# Paste the *entire content* of the downloaded JSON key file here.
GOOGLE_SHEETS_SERVICE_ACCOUNT_CREDENTIALS_JSON='{"type": "service_account", ...}' 
SPREADSHEET_ID='spreadsheet id'